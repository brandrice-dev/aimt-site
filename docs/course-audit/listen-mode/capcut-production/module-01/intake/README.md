# CapCut intake — Module 1 (two parts)

Module 1 needs two CapCut passes (split at the first checkpoint boundary
— CapCut refuses Enhance Voice over 15:00, and the full module runs
~16:32). Drop both exports here, named exactly:

```
module-01-capcut-master-part-a-processed.flac
module-01-capcut-master-part-b-processed.flac
```

FLAC is preferred (CapCut has exported lossless FLAC successfully before);
WAV is also accepted. If CapCut produces a different actual format under
that base name, that's fine — Claude inspects the real format before
processing rather than trusting the extension.

The old single-file name (`module-01-capcut-master-processed.flac`) is no
longer used — that was for the full 14-chunk master, which is now
historical/pre-limit evidence only.
