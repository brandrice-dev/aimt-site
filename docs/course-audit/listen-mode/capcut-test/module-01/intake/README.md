# CapCut intake

Drop the CapCut export here, named exactly:

```
module-01-capcut-proof-processed.wav
```

If CapCut cannot export WAV, save the highest-quality audio-only export
available under that same base name (its actual extension is fine — Claude
will inspect the real format before processing).
