# Module 12 — Final Exam / Course Completion / Certification — Implementation Authority

**Course:** AIMT Head Spa Certification Course
**Student-facing module number:** 12
**Approved module title:** Final Certification Assessment (student-facing "Module 12 · Final Exam")
**Governed by:** `docs/course-audit/00-aimt-certification-assessment-standard.md` (AIMT Certification & Assessment Standard, Version 1 — architecture locked) and `docs/course-audit/modules/module-12-final-exam-raw-blueprint.md` (Head Spa-specific competency map, critical-domain architecture, and state design — architecture locked, exam wording still raw/unapproved).
**Audit date:** August 26, 2026
**Implementation date:** August 26, 2026 (engine); August 26, 2026 (content installation); August 26, 2026 (traceability remediation); August 26, 2026 (visual/UX refactor); August 27, 2026 (Part III conversation-progression defect fix, this update)
**Implementation:** certification-assessment ENGINE + Module 12 UI architecture implemented. Production exam content is **FULLY TRACEABILITY-VERIFIED — 141/141 assessment assets (120 Knowledge, 12 Applied Cases, 9 Practitioner Conversations), 0 BLOCKED.** The three items blocked in the initial installation pass (`M02-005`, `M07-006`, `M08-012`) were replaced with owner-approved wording, each independently re-verified against the current approved Module 2/7/8 content, and are now active. See "Content status" below.
**Status:** Module 12 — assessment architecture/content complete; visual/UX refactor complete; Part III conversation-progression defect fixed. Pending owner QA of Part III + Pass/Not-Yet-Passed, then migration and live Cadence environment. NOT manually approved. NOT launch-ready.
**Production source of truth:** `headspa-mastery.html` (`module12Wrap`, technical slot `12`, view routing) + `assets/js/module12-certification.js` (client controller — visually/structurally refactored this pass, see "Visual/UX refactor" below; no assessment logic touched) + `functions/_lib/certification/*.mjs` (engine, now including installed `content-bank.mjs`) + `functions/api/certification/*.js` (server-authoritative endpoints) + `supabase/migrations/20260826_create_certification_assessment.sql` (schema, not yet run). Content-installation source of truth: the three LOCKED markdown authority files in `docs/course-audit/modules/` (`module-12-final-knowledge-bank.md`, `module-12-final-applied-cases.md`, `module-12-final-interview-bank.md`), `scripts/build-module12-assessment-bank.mjs` (generator), and [`module-12-content-traceability.md`](module-12-content-traceability.md) (per-item audit record). Owner visual QA source of truth: `scripts/review-module12-bank.mjs --browser` (local-only real-renderer visual QA server, now with full production style parity — see "Owner visual QA," below).

This document does not authorize marking Module 12 manually approved or deploying/merging to `main`. It records what was actually built — engine, then content installation, then this visual/UX refactor — so the next task (owner QA + environment integration) has an accurate implementation map.

## Visual/UX refactor (August 26, 2026 — Step 92)

Owner visual QA of the installed content found the student experience "looks like functionality inserted into the course rather than a deliberately designed certification experience" — text-heavy, over-boxed, a permanent 40-question grid dominating Part I, generic browser-chrome answer controls, and Part III not feeling distinct from the structured test. This was a **presentation-only refactor**: no assessment content, scoring, weighting, pass minimums, critical-domain rules, randomization, attempt ladder, or API contract changed. The three LOCKED bank files were not touched. Every string in `assets/js/module12-certification.js`'s `COPY` object remains byte-identical to the prior approved wording — verified by an automated diff of every string literal before/after (only new small UI labels the task itself specified verbatim, e.g. "Cadence Conversation" / "Critical Competencies" as metric-block labels, were added; nothing existing was reworded, shortened, or removed).

**Reused, not reinvented.** Every new visual primitive reuses an existing AIMT design-system token or established component pattern rather than inventing a separate "exam design system": buttons use the site's pill-CTA shape (`.sb-btn`/`.lc-btn` convention, 980px radius, dark fill); the question-map dialog, metric row, and disclosures use the same `--aimt-radius-*`/`--border2`/`--warn-light`/`--hero-bg` tokens already governing every other module; question-stem typography was corrected to exactly match `.cp-q` (the established checkpoint-question treatment: serif, 500 weight, 0.93rem) instead of an approximate, slightly-off serif size that was the actual source of the "disconnected serif" complaint; Part III's composer reuses the exact `.cp-btn`/`.voice-btn` send/voice-input pattern from every course checkpoint.

**Exam Ready (State A) rearchitected.** Three-column part overview (01/02/03, each with a one-line meta and a "What to expect" `<details>` disclosure holding the full approved paragraph copy); the five bullet-point passing requirements became a compact five-metric row (80% Overall / 75% Knowledge / 75% Applied Cases / 80% Cadence Conversation / ✓ Critical Competencies) with the full explanatory copy in a "How these are evaluated" disclosure; the checkpoint-history card became a two-sentence summary + "Read more" disclosure; "Before you begin" stayed a single `.key-point` callout. Result: the full approved copy is still present and reachable, but the initial view is dramatically shorter (verified: ~1885px total scroll height at 800px viewport width, roughly 2 screens, down from several times that).

**Part I navigation rebuilt.** The permanent 1–40 button grid is gone from the main question screen, replaced by a thin progress track/fill and "Question X of 40 · Y answered" text. A "View Question Map" link opens an on-demand native `<dialog>` (built-in focus trap + Escape-to-close in evergreen browsers) showing the full grid with a text-labeled legend (Answered/Unanswered/Flagged for review — never color-only) and direct-jump navigation. A new session-local "Flag for review" toggle (never scored, never synced to the server — organizational only) is reflected in the map and in a review-before-submission screen showing Answered/Unanswered/Flagged as clickable summary metrics that reopen the map.

**Question/answer controls redesigned.** Default `<fieldset>`/`<legend>` browser chrome is reset away while keeping full semantics (real `<fieldset>`, real `<legend>`, real radio/checkbox groups, native keyboard operation); answer choices are a custom control with a hand-drawn circular/square indicator (filled dot or square on selection — shape-based, not color-only), generous padding, and `:focus-within` focus rings — visually distinct from a bare browser radio without sacrificing accessibility.

**Part II given real case hierarchy and per-type interactions.** Scenarios sit in a left-accent-bordered panel (not a decorative card); a genuine defect found during this pass — client-quoted dialogue in the locked markdown (`> "My neck feels strained…"`) was rendering with a literal `>` character, since only `**bold**` markdown was previously converted — is now fixed the same way bold already was (the marker is stripped and the quote gets italic-serif styling matching the site's existing `.cn-text`/`.sc-text` quote convention; the quoted words themselves are untouched). Multi-select parts get an explicit "Select all that apply" label. Sequencing renders as numbered dark circular badges with up/down reorder buttons (fully keyboard-operable; drag was not implemented, so no separate keyboard alternative was needed). Classification renders each item's category choice as a segmented pill/chip control instead of generic checkboxes. Short-response textareas auto-grow from a compact starting height (matching the exact `grow()` behavior already used by every course checkpoint) and gained the same voice-input button used at every checkpoint (rendered only when `window.startVoice` is present).

**Part III made to feel like a different environment.** The structured-test white-card language recedes into one warm-neutral (`--warn-light`) rounded panel per the task's explicit direction; the composer became a real pill-textarea + circular dark send button (the exact `.cp-btn` pattern from every checkpoint) instead of a plain rectangular "Send" button; Enter now submits (Shift+Enter for a newline). No rubric criteria, numeric score, or "Question N" language is ever rendered here — confirmed by an automated static check that the file contains no reference to `rubricCriteria`/`criterionScores`/`explicitUnsafe`/`correctAnswer` anywhere.

**State C/D polish.** Pass now carries a restrained green top-border accent (`.m12x-pass-banner`); Not-Yet-Passed carries an amber top-border accent (`.m12x-notyet-banner`), never red/error styling. The Performance Review card was tightened (score moved into the header row instead of a separate line) and the "Something about your assessment doesn't look right?" card became a disclosure. No certification logic, wording, or attempt/remediation behavior changed.

**Local QA tool given full production style parity.** `scripts/review-module12-bank.mjs --browser` already extracted the real `<style>` block and linked the real `assets/css/aimt-design-system.css`; this was verified (not changed) to still work correctly against the refactored renderer — confirmed via `tests/certification-ui-refactor.test.mjs`'s live integration check that the served harness page contains real site CSS (`.sec-title` present) and the real design-system stylesheet link, and that the served renderer file is byte-identical to the production file.

**Tests — 1050 total assertions, zero regressions.** New `tests/certification-ui-refactor.test.mjs` (41/41 — static structural checks that the permanent grid is gone, the question map is a real `<dialog>` with text-labeled states, flag state never touches scoring/save calls, no-auto-submit is enforced by a separate review screen, all 5 Part II answer types render with dedicated UI, Part III never references rubric/scoring internals, all locked-copy strings are present verbatim, question typography matches the established `.cp-q` pattern, and the local QA tool serves real production CSS/renderer). All pre-existing suites (`certification-content-bank.test.mjs` 810, `certification-content-bank-sync.test.mjs` 4, `certification-local-qa-tool.test.mjs` 27, `certification-randomization.test.mjs` 27, `certification-scoring.test.mjs` 21, `certification-attempt-ladder.test.mjs` 12, the two migration suites 108 combined) remain green.

**Manual visual QA performed this pass (not a substitute for owner review).** Verified via the real `--browser` local QA server plus a scratch Review Mode harness reusing the actual `Module12Cert.render()` code path: Exam Ready hierarchy and disclosures; Part I question rendering including markdown-bold; the question map dialog (legend, states, direct jump); the review-before-submit summary; all 12 real Part II cases including CASE-01 (multi-select), CASE-05 (sequencing), CASE-07 (short-response-only), CASE-08 (classification); Part III's conversational environment, real first-name greeting, and the one-allowed-follow-up flow; State C (Pass) and State D (Not Yet Passed, including the uncleared-critical-domain warning and attempt-3 educator-remediation copy) via the fixture-state switcher. Mobile-viewport screenshot automation was unreliable in this session (the browser tool's viewport resize did not consistently apply); one successful narrow (644px) capture showed clean, non-overflowing layout, and the CSS itself uses only relative units, a `@media (max-width:720px)` grid collapse, flex-wrap navigation, and a `92vw`-capped dialog — no fixed-pixel-width elements that could overflow at phone widths. This is disclosed as a real limitation, not claimed as completed mobile QA.

---

## Part III conversation-progression defect fix (August 27, 2026 — Step 93)

Owner QA of the visual/UX refactor (Step 92) surfaced a blocking functional defect in Part III, then a second, related defect on re-review. Both were fixed together; assessment content, scoring, weighting, critical-domain logic, the one-follow-up rule, thresholds, and every locked string were left untouched.

**Defect 1 — "the same question repeats" / never reaches Conversation 2/3.** Owner-reported symptom: after answering the first Cadence prompt, the flow appeared stuck and never advanced to Conversation 2, Conversation 3, Processing, or a result.

**Defect 2 — welcome text answered as if it were the primary prompt.** A follow-up owner screenshot showed the generic Part III welcome ("You made it to the final part... Ready?") rendered with an active composer beneath it; a nonsense answer to "Ready?" was then graded against a real interview's rubric and jumped straight to that interview's approved follow-up (INT-05 observed) — its primary prompt was never shown.

**Root cause (single defect, both symptoms).** `renderPartIII()` in `assets/js/module12-certification.js` fell back to the generic welcome copy whenever a conversation's `transcript` was empty — true at the start of **every** conversation (1st, 2nd, and 3rd), not only the first. The real `interviewDef.primaryPrompt` was never rendered as a chat message, and the composer was active immediately beneath the welcome text. Reproduced against the real, unmodified renderer via `scripts/review-module12-bank.mjs --browser --seed 7`: a fresh Part III attempt showed only the generic welcome (no INT-01 primary prompt visible), and a nonsense reply to "Ready?" produced INT-01's real follow-up. This fully explains both owner reports: the "repeat" was the identical generic welcome re-appearing at the start of every conversation instead of three distinct scenarios, and the specific screenshot was that same gap swallowing the primary prompt on the very first turn. Confirmed **client-controller-only** — the server (`functions/api/certification/get-part.js`, `submit-interview-turn.js`) and the local QA harness's mock endpoints already implemented the correct sequential, ID-tracked, one-follow-up state machine; nothing there needed correction beyond the parity/robustness items below.

**Fix — server-authoritative "first conversation" signal.** New pure helper `functions/_lib/certification/interview-progression.mjs` (`findNextInterview(selectedIds, conversationState)`) determines the next not-yet-finalized interview ID and whether it is the attempt's first (`conversationIndex === 0`) — conversations are always completed strictly in the fixed selection order, so this is a reliable, cheap signal without adding a separate persisted counter. `get-part.js` now uses this helper and returns `isFirstConversation` in the Part III payload; `scripts/review-module12-bank.mjs`'s mock `get-part` does the same, so the local harness and production implement the identical rule. `renderPartIII()` now only prepends the welcome + a new "Let's start with this one." line when `conversation.isFirstConversation` is true, and **every** fresh conversation (including 2nd/3rd) always renders its own real `conversation.primaryPrompt` as a message before the composer is reachable — there is no code path where a composer goes live without the actual prompt visible above it. The Review Mode fixture conversation was updated to declare `isFirstConversation: true` (a generic welcome must never carry an interview ID, and Review Mode's single fixture is always presented as "first").

**Welcome copy replaced exactly as directed.** `COPY.partIII.openingWithName`/`openingNoName` now read: "You made it to the final part, [name]. This part is a little different. I'm going to give you a few situations you might run into in practice, and I want to hear how you'd think through them. There isn't one perfect script — just talk to me the way you normally would." followed by a new `COPY.partIII.startLine`: "Let's start with this one." The old "...We're done with multiple choice... Ready?" question-and-answer framing is gone; there is no student-response turn for the welcome itself.

**Local QA harness parity fixes (unrelated latent gaps found while verifying the real state machine end-to-end).** Three issues in `scripts/review-module12-bank.mjs` — never reachable through the real renderer's normal flow, but real deviations from the production API contract — were corrected so the harness exercises the actual state machine, not a simplified stand-in: (1) its mock `submit-interview-turn` handler was missing the `if (state.finalized) return {finalized:true, alreadyFinalized:true}` short-circuit production has, so a finalized conversation could technically be resubmitted and re-scored; (2) its mock Cadence evaluator always forced the one-allowed-follow-up path on every conversation, so the immediate-finalize path was never exercised locally — it now deterministically alternates by conversation index (even = one follow-up, odd = immediate finalize), preserving the pre-existing seed-3 integration test's expectation that conversation index 0 needs a follow-up; (3) that same mock evaluator scored every rubric criterion at `1` out of a real possible `2` (the locked 0/1/2 scale in `scoring.mjs`, unmodified), which silently capped the interview component at exactly 50% — permanently below its own 80% minimum — so a genuine Mock PASS was structurally unreachable through this harness even before this task. Criterion scores now use full credit (`2`), matching the case mock's existing "solid, professional response" standard (`mockEvaluateStructuredCasePart`'s 0.75 correctness).

**New QA-only mock-outcome control.** Per the task requirement to preview both result screens without a double 40/4/3 run, the harness banner now offers "Mock PASS" / "Mock NOT YET PASSED" / "Auto" links (`/?mockOutcome=pass|not_yet_passed|auto`). Only `not_yet_passed` changes any input — it drops the mock knowledge-percent input from 0.8 to 0.5, deliberately below `config.minimums.knowledge` (0.75), so the real, unmodified `determineCertificationDecision()` (independent per-component gates, no compensation) fails on its own logic rather than a fabricated client-side decision. Clearly labeled "QA ONLY," round-trips only through this local `GET /`, never touches `/api/certification/*`, and makes zero Supabase/production writes (same guarantee the harness already had).

**Tests — 54 new deterministic assertions, zero regressions.** New `tests/certification-part3-progression.test.mjs`: pure unit tests for `findNextInterview()` (fresh/mid-attempt/all-finalized/mid-conversation-resume); static checks that `get-part.js` and `submit-interview-turn.js` implement the fix and the finalized-lock; static checks that `renderPartIII()` gates the welcome behind `isFirstConversation` and always references `primaryPrompt`; static checks for the exact approved copy and the Review Mode fixture's `isFirstConversation: true`; static checks that the client never assigns its own `decision`/`state` (cannot self-certify); and a full integration walk against the real `--browser` harness — fresh attempt reports `isFirstConversation: true` with a real (non-generic) primary prompt, refresh-before-responding returns the same interview (no premature advance), the one-follow-up and immediate-finalize paths both work, a finalized conversation cannot be restarted, all 9 harness interviews complete exactly once with no repeats, the walk reaches `allConversationsFinalized` → Processing → a real decision, and both `mockOutcome=pass`/`not_yet_passed` reach their respective decisions. All pre-existing suites remain green, including `certification-content-bank.test.mjs`'s 120/12/9 counts and 141/141 traceability, and `certification-ui-refactor.test.mjs`'s 8/8 locked-wording spot-checks (the Part III welcome string is not among those spot-checked, so the authorized copy change didn't require updating that test) — **1104 total deterministic assertions, zero regressions.**

**Manual QA performed this pass.** Reproduced both original defects against the real renderer (`--browser --seed 7`) before fixing; after the fix, manually walked Conversation 1 (follow-up path) → Conversation 2 (immediate-finalize path) → Conversation 3 (follow-up path) in the actual browser UI, confirming each conversation shows its own distinct real primary prompt with no repeated welcome and no follow-up before its primary prompt; confirmed via the harness's fetch-driven walk that all 9 approved interviews complete exactly once; confirmed both `mockOutcome=pass` and `mockOutcome=not_yet_passed` reach their respective Processing → result screens using only the local QA control, with correct case answers (via the harness's own answer-key import) so a mis-guessed case choice couldn't confound the mock outcome.

---

## Content status — INSTALLED (August 26, 2026)

The raw Claude-generated wording in `module-12-final-exam-raw-blueprint.md` (80 knowledge questions, 8 applied cases, 8 interview prompts) remains **explicitly not approved for student-facing use** and was never copied into the production content bank, then or now.

Instead, the owner supplied three separate, LOCKED markdown authority files with student-facing wording frozen:

- `docs/course-audit/modules/module-12-final-knowledge-bank.md` (120 Knowledge questions)
- `docs/course-audit/modules/module-12-final-applied-cases.md` (12 Applied Practitioner Cases)
- `docs/course-audit/modules/module-12-final-interview-bank.md` (9 Practitioner Conversations)

A new deterministic generator, `scripts/build-module12-assessment-bank.mjs`, parses these three files programmatically (regex/line-based, never by hand-typing student-facing wording) into `functions/_lib/certification/content-bank.mjs`, which now ships `CONTENT_STATUS: 'INSTALLED'`, `bankVersion: 'headspa-fe-bank-v1-2026-08-26'`, and real `knowledgeBank`/`caseBank`/`interviewBank` arrays. `SOURCE_HASHES` embeds a SHA-256 of each locked source file at generation time; `tests/certification-content-bank-sync.test.mjs` fails if a locked file changes without re-running the generator.

**Traceability audit.** Every item was checked against the actual approved Module 1–11 specifications (`docs/course-audit/modules/module-0N.md`), not the raw blueprint and not from memory — see [`module-12-content-traceability.md`](module-12-content-traceability.md) for the full per-item record. Initial result: **117 of 120 Knowledge items VERIFIED and shipped `status:'approved'`; 3 BLOCKED and shipped `status:'draft'`** (`M02-005`, `M07-006`, `M08-012` — see "Traceability remediation" below for what was wrong with each and how they were fixed). All 12 Applied Cases and all 9 Practitioner Conversations VERIFIED and shipped `status:'approved'` from the start. `status:'draft'` items are excluded from any real student selection by `isApprovedForProduction()` — they are never silently rewritten or dropped, only quarantined and reported.

### Traceability remediation (August 26, 2026 — Step 91)

The owner supplied replacement wording for all three blocked Knowledge items. Per instruction, replacement wording was **not** auto-approved merely because the owner supplied it — each was independently re-verified against the current approved `module-02.md`/`module-07.md`/`module-08.md` content before being marked VERIFIED (full comparison in [`module-12-content-traceability.md`](module-12-content-traceability.md)'s "Remediation pass" section):

- **`M02-005`** — the new scenario matches checkpoint `m2cp1`'s exact documented scenario ("visibly stressed... apologizes for being two minutes late") and Section 2.6 "Rushing the beginning."
- **`M07-006`** — the new item ("A tool has to earn its place") matches Approved Outcomes #1/#3 (function-based, not appearance-based, equipment evaluation). This competency does not touch D4 (Sanitation/Process Integrity) at all, so its `Critical-Domain Evidence: D4` tag was **removed, not replaced with another domain** — no critical-domain tag was invented to preserve the old classification.
- **`M08-012`** — the new item tests the *current* controlling Module 8 content directly (the August 24, 2026 intake-determines-fragrance amendment), replacing wording that tested a superseded script.

**Result: 141/141 assessment assets VERIFIED, 0 BLOCKED.** Real-bank randomization was re-validated after M07-006's D4 removal (1000 seeded draws, zero failures, zero coverage warnings, D4's minimum evidence total across all draws never dropped below 3 — well above the required 2). `scripts/build-module12-assessment-bank.mjs`'s `BLOCKED_KNOWLEDGE_ITEMS` map is now empty; its history is recorded in a code comment rather than deleted silently.

### Owner visual QA (August 26, 2026 — Step 91)

`scripts/review-module12-bank.mjs --browser` launches a `127.0.0.1`-only local HTTP server that serves `assets/js/module12-certification.js` **completely unmodified** (the real production renderer, not a second implementation) against the real installed content bank, backed by local in-memory mock endpoints implementing the exact `/api/certification/*` REST contract (no Supabase, no Anthropic/`ANTHROPIC_API_KEY`, no attempt/certificate/remediation/review record ever created). Part II intentionally shows **all 12** cases and Part III **all 9** conversations in one session (the real exam draws only 4/3) so the owner can browse every item through the real renderer. Case/interview scoring for visual purposes uses a clearly-banner-labeled MOCK evaluator (always demonstrates the one allowed follow-up using the interview's real, human-authored follow-up text) — deterministic objective scoring (single-best-answer/multi-select/sequencing/classification) uses the real, unmodified `scoring.mjs` engine. A separate `/debug` route serves the full bank with answer keys/rubrics, visibly banner-labeled "INTERNAL OWNER QA ONLY — never publish, deploy, or embed." Verified end-to-end via real browser interaction: Part I's 40-question draw (including the markdown-bold rendering fix rendering **bold** text correctly instead of literal asterisks), all 12 Part II cases including CASE-08's classification UI, and Part III's real greeting/follow-up/first-name substitution. See `tests/certification-local-qa-tool.test.mjs` for the automated version of these checks.

**Engine extensions required (not exam-content changes).** The engine built in the prior task (against an empty bank) needed two small, necessary additions once real content revealed answer shapes it hadn't yet needed to support: a `classification` `CasePart` type (`content-schema.mjs`, `scoring.mjs` — CASE-08's "classify each item" part) and a `choiceIncludes` critical-flag trigger for multi-select parts (`scoring.mjs` — CASE-04's "flag this specific unsafe multi-select option regardless of what else was picked"). Neither changes any scoring philosophy, weighting, or gate rule from the prior task's locked design.

**Client UI extensions.** `assets/js/module12-certification.js`'s Part II renderer previously rendered every non-short-response case part as a generic checkbox list (correct only by accident for multi-select, structurally wrong for single-best-answer/sequencing/classification, and with no rendering path at all for the latter two) — this was harmless while the bank was empty but would have broken on real content. It now dispatches per part type: radio buttons for single-best-answer, checkboxes for multi-select, an accessible up/down-reorderable list for sequencing, and a per-item category selector for classification; a case's submit button is disabled with a visible hint until every part has a response, since submission permanently locks the case. Multi-line and markdown-bold-aware prompt/scenario rendering was also added — several Knowledge items and Case scenarios contain bulleted history lists, blockquoted client dialogue, or **bold** emphasis that the prior bare `esc()`-only rendering (built and only ever tested against single-line fixture content) would have collapsed into a run-on sentence with literal, unrendered asterisks.

**Real-bank validation.** 500+ seeded draws of `assembleAttempt()` against the real installed bank all succeed: exactly 40/4/3 selected, full Module 1–11 coverage, no duplicate IDs, and every one of D1–D4 meeting the ≥2-total/≥1-non-Part-I evidence rule with zero coverage warnings. `tests/certification-content-bank.test.mjs` (806 assertions) and `tests/certification-content-bank-sync.test.mjs` (4 assertions) are new; the pre-existing `tests/certification-randomization.test.mjs` (27), `tests/certification-scoring.test.mjs` (21), `tests/certification-attempt-ladder.test.mjs` (12), and the two migration suites (108 combined) all remain green — 978 total assertions, zero regressions.

**As a direct consequence of content now being installed, a real Module 12 attempt can be completed end-to-end once the remaining environment steps below are done** — but `issue-certificate.js` still correctly requires a server-authoritative passing `certification_attempts` row (see "Certificate issuance," below), and no attempt can be recorded until the Supabase migration is run and `ANTHROPIC_API_KEY` is provisioned (neither was done by this task — see "Explicitly not done," below).

---

## Module 12 state machine (implemented)

Four states, matching `module-12-final-exam-raw-blueprint.md` Part 5, resolved server-side by `GET /api/certification/get-status` and rendered by `assets/js/module12-certification.js`'s `Module12Cert.render()`:

| State | Trigger (server-derived) | UI |
|---|---|---|
| **A — Exam Ready** | Eligible, no attempts yet | Full "Final Certification Assessment" overview + Start Final Exam |
| **B — Exam In Progress** | An attempt with no `certification_decision` exists | Resumes at whichever part/sub-state the attempt's `status` column indicates |
| **C — Certification Earned** | Any attempt has `certification_decision = 'pass'` | Existing `module12Wrap` completion/certificate content, reused verbatim, preceded by a Performance Review summary |
| **D — Standard Not Yet Met** | Most recent finalized attempt is `not_yet_passed` and certification not yet earned | Performance Review + attempt-specific copy/actions per the ladder |

`module12Wrap`'s existing markup (curriculum-approved completion/certificate experience) was **not rewritten** — it stays exactly where it is in `headspa-mastery.html` (lines ~7681–7777), still `display:none` by default. `Module12Cert.render()` reads `document.getElementById('module12Wrap').innerHTML` and only injects it into the live view when state is C. A student opening Module 12 for the first time never sees it, per the master instructions' explicit requirement.

**View routing change (surgical):** `headspa-mastery.html`'s `STATIC_MODULES[12]` now calls `window.Module12Cert.render(wrap)` instead of unconditionally copying `module12Wrap`'s HTML in. A missing/failed script load falls back to the old unconditional behavior rather than breaking the page.

---

## Eligibility (implemented)

`functions/_lib/certification/auth.mjs`'s `hasCompletedInstructionalModules()` reads the student's server-synced `course_progress.state.progress['0'..'11'].complete` directly — the same fields the client's own `APP_STATE.isModuleComplete()` derives from — rather than trusting a client-submitted flag or approximating from `progress_score`. Prior checkpoint history is never re-graded into the exam; it only establishes this eligibility gate, per standard Section 6.

---

## Three-part assessment engine (implemented)

### Versioned configuration
`functions/_lib/certification/assessment-config.mjs` defines `HEAD_SPA_ASSESSMENT_CONFIG` (assessment version `headspa-fe-assessment-v1`) — weights (50/30/20), independent minimums (75/75/80, overall 80), difficulty targets (20/60/20), required module coverage (1–11), Part I/II/III target counts (40/4/3), critical-domain coverage rule, and attempt-ladder rule constants. `getAssessmentConfig(version)` looks up a frozen historical config by version string so a future revision never rewrites what an earlier attempt was actually held to (standard Section 15) — add a new entry, never mutate an existing one.

`functions/_lib/certification/critical-domains.mjs` defines the four locked Head Spa domains (D1 Professional Scope/Diagnosis/Referral, D2 Contraindication/Client Safety, D3 Consent/Touch/Bodywork Authority, D4 Sanitation/Process Integrity) as versioned configuration, each with a `typeBThreshold` (2) — never as scattered one-off conditionals.

### Content data contracts
`functions/_lib/certification/content-schema.mjs` defines the Knowledge/Case/Interview item shapes exactly per the task's suggested contracts, plus `projectKnowledgeItemForClient()` / `projectCaseForClient()` / `projectInterviewItemForClient()` — the only functions permitted to shape what reaches the browser. None of them ever include `correctChoice`, `rationale`, `correctAnswer`, `scoring`, `criticalFlags`, or rubric guidance.

### Constrained randomization
`functions/_lib/certification/randomization.mjs`:
- `selectPartIIAndIII()` — greedy set-cover selection of cases then interviews against the four critical domains, then fills remaining slots for module/variety coverage, preferring unseen items.
- `selectPartI()` — guarantees every required module gets ≥1 item, tops up critical-domain evidence to the required minimum (accounting for what Part II/III already covered), then fills the remaining slots targeting the ~20/60/20 difficulty mix.
- `assembleAttempt()` — orchestrates both, verifies no duplicate IDs across the whole attempt, and reports a per-domain evidence matrix + warnings.
- Explicitly returns `{ ok:false, reason:'insufficient_bank', ... }` rather than throwing when the bank can't satisfy coverage — this is the path production hits today.
- Tested against a synthetic fixture bank in `tests/certification-randomization.test.mjs` (27 assertions, all passing) — module coverage, difficulty mix, no-duplicate-IDs, domain coverage (≥2 total, ≥1 non-Part-I per domain), missing-module/too-small-bank failure handling, and retake-overlap minimization.

### Scoring + critical-domain gating
`functions/_lib/certification/scoring.mjs` implements the standard's Section 5.2 bar exactly:
- **A single missed multiple-choice question tagged as domain evidence never by itself fails a domain.** Part I evidence points are constructed with `explicitUnsafe` hardcoded `false` — that flag can only ever originate from a case/interview evaluator's structured output.
- **Type A (explicit unsafe reasoning)** — set only when a case's deterministic `criticalFlags` rule matches the student's choice, or when Cadence's structured interview/case evaluation explicitly returns it against a human-authored rubric criterion's `explicitUnsafeRule`.
- **Type B (repeated pattern)** — requires ≥`typeBThreshold` (2) independent evidence points sharing the same `patternTag`, which can originate from a knowledge item's optional `distractorPatternTags`, a case's evaluator output, or an interview's evaluator output.
- `determineCertificationDecision()` evaluates all five gates (overall, knowledge, appliedCases, interview, criticalDomains) completely independently — proven by a test where meeting every per-component minimum exactly (75/75/80) still fails because it weights out to 76% overall, and by a test where a perfect score everywhere still fails on one uncleared domain.
- Tested in `tests/certification-scoring.test.mjs` (21 assertions, all passing).

### Attempt ladder
`functions/_lib/certification/attempt-ladder.mjs`'s `determineNextAttemptEligibility()` is a pure function (no I/O) implementing standard Section 8's exact ladder — Attempt 1→2 with no extra gate, Attempt 2→3 gated on remediation completion, Attempt 3→4 gated on educator authorization, Attempt 4 unsuccessful → `individual_aimt_review` with no automatic Attempt 5, **plus** the domain-specific critical-domain remediation gate that applies starting after Attempt 1 independent of the numbered gates. `buildRemediationAssignments()` groups deficiencies by competency/critical-domain, never one row per missed question. Tested in `tests/certification-attempt-ladder.test.mjs` (12 assertions, all passing), including a case proving that remediating the *wrong* domain does not unlock the next attempt.

---

## Server-authoritative backend (implemented, release-blocker addressed)

Per standard Section 16, this repository's existing identity pattern (`Authorization: Bearer <supabase access token>` → `GET {SUPABASE_URL}/auth/v1/user` with the service-role key → entitlement check against `course_entitlements`) is reused exactly, factored into `functions/_lib/certification/auth.mjs`. No parallel auth system was introduced.

### Schema (additive) — `supabase/migrations/20260826_create_certification_assessment.sql`
- `certification_attempts` — one row per attempt: versioning columns (`assessment_version`, `standard_version`, `bank_version`), per-part selected IDs/responses/state, component scores, `critical_domain_results`, `certification_decision`, `decision_at`.
- `certification_remediation_assignments` — grouped by competency/domain.
- `certification_educator_requests` — MVP manual-scheduling record; an educator authorizes Attempt 4 by setting `attempt4_authorized` directly (out of band, not via a student-facing endpoint).
- `certification_review_requests` — human review/appeal MVP.

All four tables: RLS `select` policy restricted to `user_id = auth.uid()`, **no insert/update policy for `authenticated`/`anon`** — matching the existing `completions` table's trust model exactly. Every write goes through the service-role key inside `functions/api/certification/*.js`. A student cannot alter their own score, critical-domain result, or certification decision by writing to Supabase directly.

**This migration is committed for record-keeping only — it has not been run.** Per `CLAUDE.md`, it must be executed manually in the Supabase SQL editor before any of the endpoints below can function.

### Endpoints — `functions/api/certification/*.js`
`start-attempt` (resolve identity/entitlement/eligibility, enforce the ladder, assemble + persist a new attempt) · `get-part` (fetch Part II/III client-safe content for an active attempt) · `save-progress` (Part I/II autosave, locked-part writes silently ignored) · `submit-part1` (server-side knowledge scoring, locks) · `submit-case` (deterministic + Cadence-evaluated case scoring, locks a case, locks Part II once all cases are in) · `submit-interview-turn` (Cadence conversation turn, max one follow-up, locks a conversation, locks Part III once all conversations are in) · `finalize-assessment` (weighted overall + critical-domain gating + certification decision + remediation-assignment generation, idempotent) · `get-status` (the single source of truth `Module12Cert.render()` reads) · `request-educator-remediation` · `request-review`.

`functions/_lib/certification/cadence-grader.mjs` calls the Anthropic Messages API directly (fetch only, its own `ANTHROPIC_API_KEY` env var) rather than routing through the client-facing `headspa-proxy` Worker — rubrics and scoring logic must never reach the browser, and the Worker's contract is designed for client-issued, client-visible checkpoint prompts. Evaluator failures preserve the student's submitted response and return a retriable error rather than silently locking, losing the answer, or falsely scoring (task requirement #40).

### Certificate issuance hardened (release-blocker)
`functions/api/issue-certificate.js` gained one additional check (Step 4b, between the existing progress-score gate and issuance): a `certification_attempts` row with `certification_decision = 'pass'` must exist for the student/course, or issuance is refused with `409`. This is **in addition to**, not instead of, the existing modules-0–11 progress gate. Because the production content bank is empty, this check can never currently pass — certificate issuance is correctly, verifiably blocked until real exam content ships and a real student passes.

---

## Review Mode (implemented, isolated from production data)

Module 12 Review Mode does **not** call any of the endpoints above at all — it renders entirely from a local fixture set in `assets/js/module12-certification.js` (`fixtureStatusFor()`), selected via a visible dev-only state-switcher bar shown only when `window.ReviewMode.isActive()`. This guarantees, by construction rather than by a server-side Review-Mode check: no authoritative attempt record is ever written, no certificate is ever issued, no remediation/educator/review request is ever created, and no real attempt is ever consumed. All 10 required fixture states are covered: Exam Ready, Part I, Part II, Part III, Processing, Pass, Attempt 1 not passed, Attempt 2 remediation, Attempt 3 educator, Attempt 4 individual review.

---

## Accessibility (implemented, not yet manually verified)

Semantic `<fieldset>`/radio groups for Part I choices, labeled textareas for structured responses, `aria-live="polite"` on the question-progress indicator and the chat transcript, visible focus inherited from the page's existing foundation, no color-only correctness signaling (jump-grid "answered" state pairs color with an `aria-label` stating "answered"/"unanswered"), no countdown/timer anywhere, reduced-motion respected (no new animation was introduced). **Not yet verified:** screen-reader pass, physical-keyboard pass, real touch-device pass — same deferred category as every other module in this course.

---

## Deterministic tests (all passing)

`tests/certification-randomization.test.mjs` (27/27), `tests/certification-scoring.test.mjs` (21/21), `tests/certification-attempt-ladder.test.mjs` (12/12), `tests/certification-content-bank.test.mjs` (810/810 — content counts/IDs/shape/traceability-status/security/1000-seeded-draw selection), `tests/certification-content-bank-sync.test.mjs` (4/4 — locked markdown ↔ generated bank hash sync), `tests/certification-local-qa-tool.test.mjs` (27/27 — the local visual QA server never references Supabase/Anthropic/production endpoints, binds to 127.0.0.1 only, and its live responses contain zero answer-key leakage; the production client bundle embeds no bank content or scoring logic), `tests/certification-ui-refactor.test.mjs` (41/41), `tests/certification-part3-progression.test.mjs` (54/54 — Part III conversation-progression defect regression, see "Part III conversation-progression defect fix" above) — run via `node tests/<file>.mjs`, dependency-free (Node built-ins + ESM only). `tests/fixtures/certification-fixture-bank.mjs` contains synthetic, clearly-labeled placeholder content (never the raw blueprint's wording, never installed into `content-bank.mjs`). Pre-existing suites (`module-09-migration.test.js` 74, `module-11-relocation-migration.test.js` 34) re-run clean — **1104 total assertions, zero regressions.**

---

## Explicitly not done by this task (visual/UX refactor, August 26, 2026)

- No assessment content, scoring, weighting, pass minimums, critical-domain rules, randomization, attempt-ladder, or API-contract logic was changed — this was a presentation-layer-only pass.
- The three LOCKED markdown bank files were not touched; no student-facing question/case/interview wording was rewritten, paraphrased, shortened, or removed anywhere in `assets/js/module12-certification.js`'s `COPY` object (verified by an automated before/after string-literal diff).
- Real device-lab responsive QA was not performed — mobile-viewport screenshot automation was unreliable in this session (see "Manual visual QA," above, for exactly what was and wasn't verified).
- Screen-reader, physical-keyboard, and live-model-Cadence QA were not performed — same deferred category as every other module in this course.
- The Supabase migration was **not** run against the live database; `ANTHROPIC_API_KEY` was **not** provisioned.
- No merge to `main`, no deployment, no push.

## Explicitly not done by this task (Part III conversation-progression defect fix, August 27, 2026)

- No assessment content, scoring rule, weighting, pass minimum, critical-domain logic, randomization, or attempt-ladder behavior changed — the fix is confined to the client's conversation-display logic, a shared server-side "which conversation is next" helper, and the local QA harness's mock fidelity.
- The only student-facing wording changed is the Part III welcome (and the new "Let's start with this one." line), replaced with the owner's exact directed text — no other locked string in `COPY` was touched.
- The Supabase migration was **not** run against the live database; `ANTHROPIC_API_KEY` was **not** provisioned; no manual QA against the live environment (real Supabase-backed attempt, live-model Cadence grading) was performed.
- No merge to `main`, no deployment, no push.

## Next task

**OWNER QA OF PART III + PASS/NOT-YET-PASSED, THEN MIGRATION.** With the Part III conversation-progression defect fixed and both mock result screens now reachable locally, the owner should re-verify Part III end-to-end and both outcome screens — `scripts/review-module12-bank.mjs --browser` gives a localhost-only way to do this against the real, unmodified renderer (regenerate Part I by seed, browse all 12 real cases, all 9 real conversations, and force either `Mock PASS` or `Mock NOT YET PASSED` from the harness banner) without touching production data or requiring the live environment; the existing in-browser Review Mode fixture-state switcher remains available for exercising State A/B/C/D flow and copy. Only after the owner approves this pass should the remaining environment steps (run the Supabase migration; provision `ANTHROPIC_API_KEY`) proceed, followed by a full live-environment QA pass (real attempt, live Cadence grading, screen-reader, physical-keyboard, real touch-device) before Module 12's status may become "Implemented — manual QA approved."
